{
  "$schema": "https://opencode.ai/config.json",
  "model": "github-copilot/gpt-4o",
  "agent": {
    "plan": {
      "mode": "primary",
      "description": "Planning and analysis without making changes, while leveraging subagents to optimize operations. During more moderate and complex plans, write the plan to a Markdown formatted file in the current working directory.",
      "model": "github-copilot/claude-sonnet-4.5",
      "temperature": 0.1,
      "tools": {
        "write": true,
        "bash": false
      }
    },
    "build": {
      "mode": "primary",
      "description": "Full development work with all tools enabled, while leveraging subagents to optimize operations",
      "model": "github-copilot/gpt-4o",
      "temperature": 0.3,
      "tools": {
        "write": true,
        "edit": true,
        "bash": true
      }
    }
  }
}
