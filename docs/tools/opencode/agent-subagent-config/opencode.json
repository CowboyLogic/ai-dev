{
  "$schema": "https://opencode.ai/config.json",
  "model": "github-copilot/gpt-5.6-terra",
  "agents": {
    "plan": {
      "mode": "primary",
      "description": "You must use subagents to plan and analyze without making changes. During more moderate and complex plans, write the plan to a Markdown formatted file in the current working directory.",
      "system": "{file:./prompts/plan.txt}",
      "model": "github-copilot/claude-sonnet-5",
      "permissions": [
        { "action": "edit", "resource": "*", "effect": "allow" },
        { "action": "shell", "resource": "*", "effect": "deny" }
      ]
    },
    "build": {
      "mode": "primary",
      "description": "Full development work with all tools enabled, while leveraging subagents to optimize operations",
      "model": "github-copilot/gpt-5.6-terra"
    }
  }
}
